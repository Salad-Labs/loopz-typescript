/**
 * Represents the configuration settings for a order.
 */
type OrderConfig = {
  /**
   * @property {number} [minBlocksRequired] - The minimum number of blocks required for consider the tx valid.
   */
  minBlocksRequired?: number
}

export { OrderConfig }
